#include <stdio.h>
int main() {
    int x,y;
    scanf("%d %d",&x,&y);
    if(y==0)
    {
        printf("second number cannot be zero");
        y=1;
    }
    int r=x/y;
    printf("%d",r);
     return 0;
}
