#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int n,r,s;
    scanf("%d",&n);
    s=0;
    while(n>0)
    {       
        r=n%10;
        if(r%2==1)
        {
            s=s+r;
        }
      n=n/10;
        }
     printf("%d",s);
 return 0;
}
