#include<stdio.h>  
 int main()    
{ 
    int i,n,c;
    scanf("%d",&n);
    for(i=1;i<n;i++)
    {
    if(n%i==0)
    c=c+i;
    }
    if(c==n)
        printf("Perfect number");
        else
        printf("Not a Perfect number");
return 0;
}
